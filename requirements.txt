# ── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pandas>=2.0.0
PyYAML>=6.0
python-multipart>=0.0.9

# ── LlamaIndex PandasQueryEngine ──────────────────────────────────────────────
llama-index-core>=0.10.0
llama-index-experimental>=0.1.0       # provides PandasQueryEngine
polars>=0.20.0                         # required by llama-index-experimental

# ── LLM backends (install the one you need) ───────────────────────────────────
llama-index-llms-llama-cpp             # local GGUF model (requires llama-cpp-python)
# llama-index-llms-openai              # uncomment if using OpenAI instead

# ── CLI schema tool ───────────────────────────────────────────────────────────
rich>=13.0                             # colored terminal output for schema_tool.py
