# ── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pandas>=2.0.0
PyYAML>=6.0
python-multipart>=0.0.9

# ── LlamaIndex PandasQueryEngine ──────────────────────────────────────────────
llama-index-core>=0.10.0
llama-index-experimental>=0.1.0       # provides PandasQueryEngine

# ── LLM backends (install the one you need) ───────────────────────────────────
# Local GGUF model (LlamaCPP / Qwen2.5):
#   pip install llama-index-llms-llama-cpp
#   (may need: CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python --force-reinstall)
#
# OpenAI:
#   pip install llama-index-llms-openai
#   export OPENAI_API_KEY=sk-...

# ── CLI schema tool ───────────────────────────────────────────────────────────
rich>=13.0                             # colored terminal output for schema_tool.py
